name: Infra Apply (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled Workflow
        run: |
          echo 'This workflow is disabled.'
          echo 'Use .github/workflows/infra-apply-oci.yml instead.'
          exit 1
